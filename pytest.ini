[pytest]
# Put the project root on sys.path so tests can import agent, logic_utils and
# evaluate regardless of how pytest is launched (bare `pytest` or `python3 -m pytest`).
pythonpath = .
testpaths = tests
